function varargout = runtimeErrorHandler(varargin)
% Will deal with the catched errors and log them to a file (different to
% the diary log_all.log which saves everything that's displayed on the
% command window.
%
% TODO